numpy>=1.24
pyyaml>=6.0
tqdm>=4.66
pytest>=8.0

# Optional for real model-backed runs. The mock backend used by tests does not
# require these packages.
torch>=2.1
transformers>=4.40
modelscope>=1.14
datasets>=2.18
peft>=0.10
